package WrongApproach;

public class CalculateSum {
	public int Sum(int firstValue, int secondValue) {
		if(firstValue!=' ' && secondValue != ' ') {
			return firstValue + secondValue;
		}else {
			return 0;
		}
	}
}
